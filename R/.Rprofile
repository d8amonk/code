# Sample Rprofile.site file 

# Things you might want to change
# options(papersize="a4") 
# options(editor="notepad") 
# options(pager="internal")

# R interactive prompt 
# options(prompt="> ")
# options(continue="+ ") 

# to prefer Compiled HTML 
# help options(chmhelp=TRUE) 
# to prefer HTML help 
# options(htmlhelp=TRUE) 

# General options 
options(tab.width = 2) 
options(width = 130)
options(graphics.record=TRUE) 

.First <- function(){
 # library(Hmisc)
 # library(R2HTML)
 cat("\nHey, Jeff!! It's ", date(), "\n")
 setwd("/Users/jvg/Google Drive/Code/R") 
 source("r_colors.R")
}

.Last <- function(){ 
 cat("\nGoodbye!! It's ", date(), "\n")
}